# 原型归档状态

以下 HTML 文件已归档于 `docs/design/`，是 LightCode 的**已批准视觉基线**。它们用于校验布局、信息层级和交互表达；它们不是 Vue 运行时代码，也不能证明任何后端能力已实现。

| 文件 | 状态 | 视觉与交互约束 |
| --- | --- | --- |
| `agent-workspace.html` | 已归档 | 左侧会话栏、中央执行流、可调整大小的右侧抽屉。`generate_diff` 行保持紧凑，完整 Diff 仅在审查抽屉。 |
| `workspace-home.html` | 已归档 | 首页仅显示最近工作区；全部已注册工作区位于可搜索的右侧抽屉。 |
| `session-history.html` | 已归档 | 紧凑时间线与右侧只读详情抽屉；待审批任务通过“继续审查”返回工作区。 |
| `settings.html` | 已归档 | 两列设置中心与分类导航；运行时、权限和命令策略文案必须与实际阶段能力一致。 |

## 使用规则

1. 原型文件必须保留在本目录，不能复制到 `frontend/src/`。
2. Vue 实现必须使用组件、Pinia、类型化合约与服务接口重建，不得整体粘贴原型 HTML。
3. 架构文档决定行为和安全边界；原型决定视觉语言。二者冲突时，以架构为行为依据。
4. 新的视觉或交互决策需要同时更新本文件、`README.md` 和相关架构文档。
5. Phase 0.5 的原型中出现的读文件、写文件、运行测试或模型输出仅为产品流程表达；当前 Runtime 仍是确定性 Mock。Phase 1 的真实能力必须遵守 `../phase1-safety-contract.md`。
