# Node and Vue
node_modules/
dist/
.vite/

# Python runtime, reserved for Phase 0.5+
*.py[cod]
.venv/
venv/

# Local runtime data, reserved for later phases
backend/data/*.db
backend/data/*.db-shm
backend/data/*.db-wal
backend/backend/data/*.db
backend/backend/data/*.db-shm
backend/backend/data/*.db-wal
backend/.env
frontend/.env
*.log

# Phase 1: static workspace registration config points to real local paths;
# it must never be committed (contains machine-specific rootPath values).
backend/workspaces.json

# IDE and operating system state
.idea/
.vscode/
.DS_Store
Thumbs.db

# Agent
.workbuddy
.trae
.superpowers
.codex
.opencode

# Skill 管理（2026-08-12）: 受控技能数据目录（ZIP 与 SKILL.md 均不入库）
backend/data/skills/
